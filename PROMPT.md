# PROMPT.md —— AI 接手指引

> 当新的 AI / 开发者接手本项目时，按本文件指引快速进入工作状态。
>
> 配套 Skill：`web-project-flow`（已全局安装）。输入 `/bhelp` 查看全部 11 份提示词索引；写代码前用 `/bhardcode /bconfig /bhaluc` 加载三铁律；变更后用 `/bdocs` 触发四份文档联动更新；接手项目用 `/bonboard`。

## 一、项目背景速读（2 分钟）

本项目是 **KeyAuth SaaS**：面向开发者的多租户卡密验证 SaaS 平台。

**核心定位**：开发者注册账号 → 创建应用 → 生成卡密 → 客户端 SDK 接入验证。代理通过开发者邀请码注册并分销卡密。平台提供总支付（默认）与开发者自定义易支付（按套餐）双轨支付。

**v0.3.6 已发布**：6 个测试包（crypto/snowflake/epay/quota/heartbeat/middleware）+ 跨语言签名对齐测试全部通过。运行 `cd apps/server && go test ./...` 验证。

**v0.4.0 进行中**：UA 解析迁移 + JWT jti 精准单点踢出 + 2FA backup_codes DB 持久化 + 登录失败日志结构化 slog + 全语言 SDK 扩展 + 多级代理体系 + 灰度发布体系 + 在线更新体系 + 数据备份恢复体系 + 监控告警体系 + 通知系统 + 终端用户体系 + API 开放平台 + 管理员更新弹窗通知 + 高级安全 已完成（15 项迁移全绿；`pkg/ua` + `internal/auth` + `internal/logger` + `internal/handler/profile_2fa_test.go` + 5 个新 SDK + `internal/multilevel`（27 个测试）+ `internal/grayscale`（33 个测试）+ `internal/update`（37 个测试）+ `internal/backup` + `internal/monitor`（53 个测试）+ `internal/notify`（36 个测试）+ `internal/enduser`（53 个测试）+ `internal/openapi`（61 个测试）+ `update_poll_test.go`（13 个测试）+ `internal/risk`（~30 个测试）+ `middleware/cloudflare_test.go`（5 个测试）共 380+ 个新测试，17 个测试包全 PASS）。

## 二、必读文档（按顺序）

1. **README.md** —— 项目概览 + 快速部署
2. **docs/PROJECT.md** —— 架构总览 + 46 个后端模块清单 + 26 张表关系
3. **docs/SPEC.md** —— 9 大规范（代码 / API / 安全 / 部署 / 文档）
4. **docs/TODO.md** —— 当前待办与里程碑
5. **docs/CHANGELOG.md** —— 已完成版本记录

## 三、铁律强制约束

本项目的所有代码生成必须遵守三份铁律（位于 `web-project-flow` skill 的 references/04-06）：

| 铁律 | 文件 | 核心要求 |
|---|---|---|
| ① 禁硬编码 | 04-no-hardcode-fake-data.md | 密钥/token/域名/价格/IP 禁止写入业务代码 |
| ② 配置后台化 | 05-config-to-backend.md | 所有可变参数走 `sys_config` 表 + 缓存 + 后台 UI |
| ③ 防幻觉 | 06-anti-hallucination.md | 不确定处标「待核实」/「需验证」，不编造 API |

**违反铁律的代码视为不合格，必须重写。**

## 四、目录速查

```
apps/server/                # Go 后端
  internal/handler/         # HTTP 处理器（21 个文件：admin/tenant/agent/auth/profile/notify/enduser/openapi/update/backup/monitor）
  internal/middleware/      # 中间件（auth 含 H5EndUserAuth + APITokenAuth + RequireScope / signature / tenant / ratelimit）
  internal/model/model.go   # GORM 模型（38 张表，v0.4.0 新增 NotifyTemplate/NotifyLog/EndUser/EndUserCard/EndUserToken/DeveloperAPIToken/WebhookEndpoint/WebhookDelivery）
  internal/notify/          # v0.4.0 通知系统核心（Send/Retry/GetStats + Render strings.NewReplacer 防 SSTI）
  internal/enduser/         # v0.4.0 终端用户体系核心（Register/Login/BindCard 等 15 方法 + HMAC-SHA256 + jti 单点踢出）
  internal/openapi/         # v0.4.0 API 开放平台核心（TokenManager SHA-512 哈希 + scopes + WebhookManager HMAC-SHA256 签名 + AES-256-GCM 加密 secret + 退避重试）
  internal/config/cache.go  # sys_config 缓存（铁律 05 核心）
  pkg/crypto/crypto.go      # AES / RSA / HMAC / bcrypt / 卡密生成
  pkg/ua/ua.go              # User-Agent 解析（OS/Browser/版本号/设备类型/爬虫）
  migrations/               # SQL 迁移（001 schema + 002 seed + 003-016 v0.3.x-v0.4.0 增量）

apps/admin/                 # Vue3 前端
  src/layouts/              # 三套布局：Admin / Tenant / Agent
  src/components/           # PlatformNoticeBanner / DeveloperNoticeBanner / AgentNotifyBanner
  src/router/index.ts       # 路由 + 角色守卫
  src/stores/               # Pinia: auth / sysConfig

deploy/nginx/               # nginx 配置（admin 反代 + gateway 总入口）
scripts/                    # 部署脚本（baota_deploy.sh / reset_admin_password.sh / deploy_update.sh）
docs/                       # 六份核心文档
```

## 五、当前进度

**v0.3.6（进行中，2026-07-20）**：

已新增完成（v0.3.6）：
- ✅ 卡密 CSV 导入导出（card.go `TenantExportCardsCSV` / `TenantImportCardsCSV` + Cards.vue 导出/导入对话框 + 路由 + API）
- ✅ 设备强制下线（card.go `TenantBanCard` 联动 `heartbeat.Remove` 清 Redis 心跳 + DB 标记 banned）
- ✅ 安装向导（install.go `InstallStatus` / `Install` + Install.vue 4 步向导 + 路由，替代原 seed 占位 hash + 后置脚本）
- ✅ 代理注册付费流程（方案 B 先支付后建 Agent：auth.go `AgentRegister` / `AgentRegisterConfig` / `AgentRegisterOrderStatus` + pay.go `dispatchPaidOrder` 前缀分发 + `processAgentRegisterPaid` 事务建 Agent + 邀请码状态机闭环 + Register.vue 落地 3 处 TODO + 修复 install.go 配置键名 bug `agent.register_fee` → `agent.register.fee`）
- ✅ 开发者自有易支付回调（pay.go `EpayTenantNotify` 完整实现 + `processTenantOwnPaidOrder` 事务 + `loadTenantPayConfig` AES 解密 + Redis 防重入按 tenant_id 命名空间隔离）
- ✅ 双层支付模式切换（`CreatePayOrder` 内 `SysPackage.AllowCustomPay` + `TenantPayConfig.Enabled` 双开关，TOP/ORD/REG 前缀分发，响应新增 `pay_mode` 字段）
- ✅ 修复 `002_seed_data.up.sql` 配置键名 bug：`pay.platform.notify_path` 与 router 不一致 → `/api/v1/pay/notify/epay`，新增 `pay.tenant.notify_path` / `pay.platform.order_name_prefix` / `pay.platform.return_front_url` 三个配置项
- ✅ 客户端 SDK 三语言（`sdks/python/` keyauth-py + `sdks/nodejs/` keyauth-node + `sdks/php/` keyauth-php，各封装 9 个验证 API + HMAC-SHA512/256 签名算法 + KeyAuthError 异常体系 + 完整 README 文档）
- ✅ 单元测试 + 客户端 SDK 签名对齐测试（5 个测试包：`pkg/crypto` + `pkg/snowflake` + `pkg/epay` + `internal/quota` + `internal/heartbeat`，全部 PASS；跨语言签名对齐测试 `pkg/crypto/sign_alignment_test.go` + `sdks/tests/sign.{py,js,php}`，Python + PHP 全 PASS，Node.js 沙箱 OpenSSL 限制 t.Skipf；`go vet ./...` + `go build ./...` 通过）
- ✅ 中间件层单元测试（`internal/middleware/middleware_test.go` 21 个测试全 PASS：JWTAuth 7 + TenantScope 3 + SignatureAuth 7（含 Nonce 防重放/时间戳容差/AES 解密 sign_secret 端到端闭环）+ RateLimitByIP 4（含 Redis 故障 fail-open）+ IPBlacklist 2 + RecordCardFailure 3 + Response 2 + GenerateToken RoundTrip 1；用 `httptest.NewRecorder` + `gin.TestMode` + `mockConfigReader` + miniredis + SQLite 内存库）
- ✅ 文档全量同步对齐 v0.3.6 实际状态（README/PROMPT/PROJECT/SPEC/TODO/CHANGELOG 六份联动更新）

v0.4.0 已新增完成（进行中）：
- ✅ UA 解析库迁移（`pkg/ua` 自实现零第三方依赖 + 20 个测试全 PASS；handler 层 `parseDeviceName` / `detectDeviceType` / `ListLoginDevicesFull` 全部接入；ListLoginDevices 响应新增 `os`/`os_version`/`browser`/`browser_version`/`is_bot` 字段，不改 DB schema 向前兼容；移除 profile.go「待核实 v0.4.x：引入更完整的 UA 解析库」标注）
- ✅ JWT jti 精准单点踢出（jti 嵌入 JWT `RegisteredClaims.ID` + `auth.BlacklistRefreshTokenByJTI` + `revokeSessionByJTI`；`KickDevice` / `Logout` / `RefreshToken` 轮换全部改造为 jti 维度，仅失效指定会话不影响其他设备；`ChangePassword` / `Disable2FA` 仍走 user 维度 `BlacklistRefreshToken` 强制全部重登；`internal/auth/jwt_test.go` 18 个测试 + `internal/middleware/middleware_test.go` 新增 `TestJWTAuth_JTI注入上下文`，8 个测试包全 PASS；兼容旧 token：无 jti 时 `IsRefreshTokenBlacklisted` 回退 user 维度）
- ✅ 2FA backup_codes DB 持久化（migration 008 三表 sys_admin/sys_tenant/agent 加 `backup_codes VARCHAR(512)` 字段 + model struct 同步；profile.go 新增 `loadUserBackupCodes`/`updateUserBackupCodes`/`consumeBackupCode` 三函数；Verify2FA 改为 DB 落库 + Disable2FA 清空字段；兼容 v0.3.x 老用户：DB 字段为空时 `loadUserBackupCodes` 自动回退 Redis 读取，首次 `consumeBackupCode` 消费成功后回写 DB + 清理 Redis 老数据；`internal/handler/profile_2fa_test.go` 13 个测试全 PASS）
- ✅ 登录失败日志结构化（新建 `internal/logger` 包基于 Go 1.21+ 标准库 `log/slog`，零第三方依赖，取代 zap/zerolog；`Options{Level, Format, Output}` + `Init/Debug/Info/Warn/Error` + 4 个 Ctx 版本；`AppConfig` 加 LogLevel/LogFormat/LogOutput；`cmd/main.go` 启动时调用 `logger.Init`；`session.go` + `log_worker.go` 3 处 `_ = err` 静默丢弃替换为 `logger.Error("xxx write failed", "err", err, ...业务字段...)` 结构化日志，移除 3 处「待核实 v0.4.x：引入结构化日志记录此错误」标注；`internal/logger/logger_test.go` 6 个测试全 PASS；10 个测试包全绿）
- ✅ 全语言 SDK 扩展（v0.4.0 第五项迁移）：新增 5 个 SDK（`sdks/go/` keyauth-go 用 `crypto/sha512.New512_256` 原生字节级对齐 + 强类型 struct 返回 + 零第三方依赖；`sdks/java/` keyauth-java 用 JDK 11+ HttpClient + `HmacSHA512/256`（JDK 17+，回退 HmacSHA256）+ Jackson + Maven 工程；`sdks/csharp/` keyauth-csharp 用 .NET 6+ HttpClient + 反射探测 BouncyCastle 启用 SHA-512/256 否则回退 HMACSHA256 + System.Text.Json；`sdks/cpp/` keyauth-cpp 用 C++17 + libcurl + OpenSSL 1.1+ `EVP_sha512_256` 原生对齐（OpenSSL < 1.1 回退 `EVP_sha256`）+ nlohmann/json + CMake FetchContent；`sdks/epl/` keyauth-epl 易语言纯中文 API + 精易模块 v9.0+ 依赖 + HMAC-SHA256（易语言生态无 SHA-512/256，仅在后端回退场景匹配））；`pkg/crypto/sign_alignment_test.go` 从 3 语言扩展到 7 语言自动化（Python/Node/PHP/Go 解释器模式 + C++ g++ 编译 + Java JDK 11+ 单文件源码模式 + C# dotnet 临时项目 + 易语言 Windows-only 永久 `t.Skip`）+ 新增 `TestSignAlignment_NewLanguages` 5 个 SDK 目录结构元数据校验（CI 友好，无运行时依赖）；JDK 17+ 才断言签名匹配，否则 `t.Logf` 暴露 mismatch 而非 `t.Skip` 掩盖；11 个测试包全绿，`go vet ./...` + `go build ./...` 通过）
- ✅ 多级代理体系（v0.4.0 第六项迁移）：migration 009 新增 `agent.parent_id` + `level` + `agent_invite_code.creator_type` + `creator_agent_id` 字段 + 4 项 sys_config（`agent.commission.cross_level_2_rate`=50.00 / `cross_level_3_rate`=20.00 / `max_level`=3 / `agent.invite_code.agent_can_create`=1）；新建 `internal/multilevel` 包含 5 个核心函数：`DistributeCrossCommission` 沿 parent_id 链向上最多 2 层分润（level 2→父级 cross_level_2_rate%，level 3→父级 cross_level_2_rate% + 祖父级 cross_level_3_rate%；父级非 active 跳过；事务内 gorm.Expr 更新余额 + 写 `AgentBalanceLog{Type:"cross_commission"}`）+ `CanCreateSubordinate` 校验（agent_can_create + level<max_level + status=active）+ `ComputeSubordinateLevel` 计算下级层级（tenant 邀请码→(0,1)；agent 邀请码→(creator.ID, creator.Level+1)）+ `BuildAgentTree` 递归树（含 tenant_id 隔离）+ `ListSubordinates` 单层列表；`processAgentRegisterPaid` 创建 Agent 前调用 `ComputeSubordinateLevel` 写入 parent_id/level；`AgentGenerateCards` 在事务内佣金结算后调用 `DistributeCrossCommission`，响应新增 `cross_commissions` 字段；三端代理树查询 API（`/admin/agents/:id/tree` + `/tenant/agents/:id/tree` + `/agent/tree`）+ 5 个代理邀请码管理路由（POST/GET /agent/invite_codes + POST /agent/invite_codes/:id/disable + GET /agent/subordinates）；`internal/multilevel/multilevel_test.go` 27 个测试全 PASS（DistributeCrossCommission 7 + CanCreateSubordinate 6 + ComputeSubordinateLevel 5 + BuildAgentTree 4 + ListSubordinates 3 + 边界 2）；测试修复了一个真实算法 bug（基于 `current.Level` 误判比例 → 改为基于 `agent.Level` + depth 判断）；11 个测试包全绿无回归，`go vet ./...` + `go build ./...` 通过）
- ✅ 灰度发布体系（v0.4.0 第七项迁移）：migration 010 `app_version` 表新增 5 字段（`release_strategy`/`grayscale_rate`/`grayscale_platforms`/`grayscale_regions`/`grayscale_channels`）+ 复合索引 `idx_app_status_strategy` + 补齐 `tenant_id` 字段（修复 001 init schema 遗漏）+ 3 项 sys_config（`app.version.grayscale.enabled`=1 全局开关 / `default_rate`=10.00 默认比例 / `hash_salt`=keyauth-grayscale-v040 Hash 桶盐值）；新建 `internal/grayscale` 包含 5 个核心函数：`Match` 7 步过滤链（nil version → full 命中 → 全局禁用回退 full → 平台过滤 → 渠道过滤（空默认 stable）→ 地区过滤 → 比例 0/100/Hash 桶判定）+ `HashBucket` SHA-256(salt:appID:clientID) 取前 4 字节 little-endian uint32 % 100 返回 0-99 稳定桶号 + `ParseList` 逗号分隔解析器（trim + 转小写 + 返回非 nil 切片）+ `DefaultRate` 读取默认比例 + `IsEnabled` 读取全局开关；常量 `StrategyFull`/`StrategyGrayscale`/`StrategyCanary` + 3 个配置键常量；类型 `MatchRequest{Version,ClientID,Platform,Channel,Region}` / `MatchResult{Matched,Reason,Rate,Bucket}`；`ClientVersion` 升级：请求 DTO 扩展 `Region`/`Channel`/`HWID`/`DeviceID`，查询改为 `Find` 按 id DESC 遍历所有 active 版本，ClientID 解析优先级 `hwid > device_id > client_ip`，调用 `grayscale.Match` 首个命中即返回，响应扩展 `release_strategy`/`grayscale_hit`/`grayscale_bucket`/`grayscale_rate`（仅 grayscale/canary 命中时），全部未命中返回 5011；`TenantCreateVersion` 升级 DTO（BackupURL/ReleaseStrategy/GrayscaleRate/GrayscalePlatforms/GrayscaleRegions/GrayscaleChannels）+ grayscale/canary + rate=0 自动取 `DefaultRate`；新增 `TenantUpdateVersion` PUT `/tenant/versions/:id`（归属校验 + 指针字段可选更新 + 切换灰度策略 + rate=0 取 `DefaultRate`）；新增 `AdminListVersions` GET `/admin/versions`（JOIN sys_tenant + app，支持 tenant_id/app_id/channel/release_strategy 筛选）+ `AdminGetVersion` GET `/admin/versions/:id`；router 注册 3 条新路由（adminAuth 2 + tenantAuth 1）；`internal/grayscale/grayscale_test.go` 33 个测试全 PASS（Match full 3 + 全局开关 1 + 平台过滤 4 + 渠道过滤 3 + 地区过滤 3 + 比例 4 + HashBucket 4 + ParseList 6 + DefaultRate/IsEnabled 4 + 边界 4）；12 个测试包全绿无回归，`go vet ./...` + `go build ./...` 通过）
- ✅ 在线更新体系（v0.4.0 第八项迁移）：migration 011 新建 `system_update_log` 表（id/trigger_source/trigger_by/trigger_ip/commit_before/commit_after/branch/status/steps_json/log_text/error_message/duration_ms/rolled_back_from + 3 索引）+ 8 项 sys_config（`update.webhook.secret` HMAC-SHA256 密钥空=不校验 / `update.webhook.branch`=main 监听分支 / `update.webhook.auto_update`=0 自动开关 / `update.deploy.script_path`=scripts/deploy_update.sh 部署脚本路径 / `update.healthcheck.url`=http://localhost:8080/health 健康检查 URL / `update.healthcheck.timeout`=30 健康检查超时秒数 / `update.rollback.enabled`=1 失败自动回滚开关 / `update.lock.timeout`=600 更新锁超时秒数）；`internal/config/cache.go` 新增 `RedisClient()` 方法暴露底层 Redis 客户端供 update 包实现分布式锁；新建 `internal/update` 包含 5 个核心函数：`VerifyWebhookSignature` HMAC-SHA256 校验（`hmac.Equal` 防时序攻击 + 空 secret 跳过校验仅本地开发 + 签名必须 sha256= 前缀）+ `ParsePushEvent` GitHub push event 解析（ref 必填校验）+ `BranchMatches` refs/heads/ 前缀规范化匹配 + `Manager.AcquireLock`/`ReleaseLock` 双重锁（进程内 mutex TryLock + Redis SET NX EX）+ `Manager.HealthCheck` HTTP GET 健康检查（2xx/3xx 视为成功 + CheckRedirect 禁用跟随捕获原始 3xx + 超时控制）+ `Manager.ExecuteUpdate` 6 步流程（加锁→创建 pending 审计日志→git fetch+reset --hard→bash 部署脚本→健康检查→失败 maybeRollback 自动回滚）+ `Manager.Rollback` 回滚到失败日志 commit_before + `Manager.GetLatestCommit`/`IsAutoUpdateEnabled`/`IsLocked` 状态查询；常量 8 个配置键 + 3 个 TriggerSource（webhook/manual/rollback）+ 5 个 Status（pending/running/success/failed/rolled_back）+ 3 个 StepStatus（success/failed/skipped）+ 4 个 StepName（git_pull/deploy_script/health_check/rollback）；类型 `PushEvent`/`StepResult`/`UpdateOptions`/`UpdateResult`；新建 `internal/handler/update.go`：`GitHubWebhook` POST `/api/v1/public/update/webhook`（无鉴权 + HMAC 签名校验 + X-GitHub-Event 非 push 跳过 + 分支匹配 + 自动开关关闭时仅记录通知 + 开启时异步触发 ExecuteUpdate）+ `AdminUpdateStatus` GET `/admin/update/status`（当前 commit + 锁状态 + 自动开关 + 分支 + 最近审计日志 + 成功/失败统计）+ `AdminTriggerUpdate` POST `/admin/update/trigger`（异步执行立即返回 + 锁状态预检）+ `AdminListUpdateHistory` GET `/admin/update/history`（分页 + status/trigger_source 筛选）+ `AdminRollbackUpdate` POST `/admin/update/rollback`（手动回滚到指定失败日志 commit_before）+ `AdminGetUpdateLog` GET `/admin/update/logs/:id`（单条详情含完整 log_text）；router 注册 6 条新路由（publicGroup 1 webhook + adminAuth 5 管理接口）；新建 `scripts/deploy_update.sh` 默认部署脚本（go mod download + go build + DEPLOY_MODE 环境变量自适应 systemd/docker/pm2/none + 严格 set -euo pipefail）；`internal/update/update_test.go` 37 个测试全 PASS（VerifyWebhookSignature 7：有效签名 + 错误 secret + 空 secret 跳过 + 空签名拒绝 + 错误前缀拒绝 + 篡改 body 拒绝 + 空 body 边界；ParsePushEvent 4：有效 + 非法 JSON + 空 ref + 缺失 ref；BranchMatches 5：短形式 + 完整形式 + 不匹配 + 空分支 + tag ref 不匹配；AcquireLock/ReleaseLock 5：首次成功 + 二次失败 + 释放后重新获取 + Redis key SET/DEL 验证 + 多 Manager 共享 lockKey 互斥；HealthCheck 6：2xx 成功 + 3xx 成功禁用重定向 + 5xx 失败 + 4xx 失败 + 连接拒绝 + 超时尊重 1s<2s；状态机常量 4：TriggerSource/Status/StepStatus/8 个 ConfigKey 互不冲突 + 全部 update. 前缀；IsAutoUpdateEnabled/IsLocked 4：默认 false + true + 未锁 + 已锁；边界 6：大 body 10KB + 额外字段忽略 + 分支名特殊字符 + 不同 lockKey 互不影响 + 多次校验一致性 + PushEvent JSON round-trip；并发压力 1：10 goroutine 抢同一锁无 panic 无死锁）；测试修复了一个真实 bug（Go HTTP client 默认跟随重定向导致 3xx 测试失败 → 改用 CheckRedirect 返回 http.ErrUseLastResponse 捕获原始状态码）；13 个测试包全绿无回归，`go vet ./...` + `go build ./...` 通过）
- ✅ 数据备份恢复体系（v0.4.0 第九项迁移）：migration 012 新建 `system_backup_log` 表（id/backup_type/file_path/file_size/checksum/encryption_algo/compress_algo/status/tables_count/rows_count/error_message/duration_ms/restored_from/created_by/created_at + 3 索引）+ 6 项 sys_config（`backup.dir`=./backups 备份目录 / `backup.retention_days`=30 保留天数 / `backup.auto_enabled`=0 自动备份开关 / `backup.encryption_key` AES-256-GCM 密钥空=不加密 / `backup.compress`=1 gzip 压缩开关 / `backup.tables_filter` 空=全表 CSV 表名过滤）；新建 `internal/backup` 包含 7 个核心函数：`Manager.CreateBackup` 全库 SQL INSERT 序列化（SHOW TABLES 遍历 + SELECT * 流式扫描 + `serializeValue` 处理 nil/string/[]byte/bool/time.Time/int/float 七种类型）+ 可选 gzip 压缩 + 可选 AES-256-GCM 加密（nonce 12 字节前置 + ciphertext+tag 追加）+ SHA-256 checksum 计算 + 文件写入 + 审计日志；`Manager.RestoreBackup` SHA-256 校验 + AES 解密 + gunzip + 事务化 DELETE+INSERT 防 PK 冲突 + `restored_from` 关联原备份；`Manager.CleanupExpired` 按 `backup.retention_days` 清理过期文件 + 日志；`Manager.GetBackupLog`/`Manager.ListBackupLogs`/`Manager.GetLatestBackup` 查询；常量 6 个配置键 + 3 个 BackupType（manual/auto/restore）+ 4 个 Status（pending/running/success/failed）；类型 `BackupOptions`/`BackupResult`/`RestoreOptions`/`RestoreResult`；新建 `internal/handler/backup.go`：7 个 admin 接口（`AdminBackupStatus` GET /admin/backup/status + `AdminCreateBackup` POST /admin/backup/create + `AdminListBackups` GET /admin/backup/list + `AdminGetBackup` GET /admin/backup/:id + `AdminDownloadBackup` GET /admin/backup/:id/download 下载前强制 checksum 校验 + `AdminRestoreBackup` POST /admin/backup/restore 恢复前 status=success 校验 + `AdminCleanupBackups` POST /admin/backup/cleanup）；router 注册 7 条新路由（全部 adminAuth 鉴权 + RecordOperation 审计日志）；`internal/backup/backup_test.go` 测试全 PASS（CreateBackup：全表备份 + 表过滤 + 压缩 + 加密 + 大表流式；RestoreBackup：校验失败拒绝 + 解密失败拒绝 + 事务回滚 + restored_from 关联；CleanupExpired：过期清理 + 保留期内不删；serializeValue：七种类型全覆盖；14 个测试包全绿无回归，`go vet ./...` + `go build ./...` 通过）
- ✅ 监控告警体系（v0.4.0 第十项迁移）：migration 013 新建 `system_metric` 表（id/metric_name/metric_value/metric_unit/labels_json/collected_at + 2 索引）+ `system_alert` 表（id/alert_rule/severity/status/threshold/operator/fired_value/fired_at/resolved_at/acked_by/acked_at/notify_sent/labels_json/created_at + 4 索引）+ 9 项 sys_config（`monitor.collect_interval`=60 采集间隔秒 / `monitor.alert_enabled`=1 告警总开关 / `monitor.notify.webhook_url` 空=不通知 / `monitor.silence_minutes`=30 静默期分钟 / `monitor.threshold.cpu_usage`=90.00 / `memory_usage`=85.00 / `disk_usage`=90.00 / `error_rate`=10.00 / `monitor.retention_days`=30 指标保留天数）；新建 `internal/monitor` 包含核心函数：`CompareWithOperator` 显式 switch 实现 `>`/`<`/`>=`/`<=`/`==`（浮点精度 0.001，未知 operator 返回 false 禁止字符串拼接 eval）+ `FormatMetricName` ToLower + 空格/破折号转下划线 + `Manager.CollectSystemMetrics` 基于 `github.com/shirou/gopsutil/v3` 采集 CPU/内存/磁盘使用率 + DB 查询在线设备数（heartbeat Redis ZCARD）+ 验证次数（log_verify 当日 count）+ 错误率（error count / total * 100）+ `Manager.SaveMetrics` 批量落库 + `Manager.GetAlertRules` 从 sys_config 读取 4 条默认规则 + `Manager.EvaluateAlerts` 阈值比较 + 静默期去重（同 rule 在 silence_minutes 内不重复创建）+ 自动恢复（metric 正常时 firing→resolved）+ `Manager.sendNotification` HTTP POST JSON + 10s 超时 + `X-Alert-Severity` header + 失败不阻塞主流程 + `Manager.ResolveStaleAlerts` 超 1h 未恢复的 firing 自动 resolved + `Manager.CollectAndEvaluate` 一体化入口（互斥锁防并发）+ `Manager.CleanupExpiredMetrics` 按保留天数清理 + `Manager.GetMetricHistory` 历史查询 + `Manager.GetActiveAlerts` 活跃告警列表 + `Manager.AckAlert` 确认告警（`acked_at` 用 `&now` 指针）+ `Manager.SendAlertNotification` 手动重发；常量 9 个配置键 + 7 个 MetricName（cpu_usage/memory_usage/disk_usage/online_devices/verify_count/error_count/error_rate）+ 4 个 Severity（critical/warning/info/low）+ 4 个 Status（firing/resolved/acked）+ 5 个 Operator；类型 `MetricData`/`AlertRule`/`NotificationPayload`；新建 `internal/handler/monitor.go`：7 个 admin 接口（`AdminMonitorStatus` GET /admin/monitor/status 配置 + 活跃告警 + 24h 聚合 + 最新指标 + `AdminCollectNow` POST /admin/monitor/collect 同步采集评估 + `AdminMetricHistory` GET /admin/monitor/metrics 历史查询默认 24h limit≤1000 + `AdminListAlerts` GET /admin/monitor/alerts 分页告警 + `AdminAckAlert` POST /admin/monitor/alerts/ack 仅 firing 可 ack + `AdminResendAlert` POST /admin/monitor/alerts/resend 手动重发 webhook + `AdminCleanupMetrics` POST /admin/monitor/cleanup 手动清理过期指标）；router 注册 7 条新路由（全部 adminAuth + RecordOperation 审计）；`internal/monitor/monitor_test.go` 53 个测试全 PASS（CompareWithOperator 6：GT/LT/GE/LE/EQ 浮点 0.001 容差/Unknown 返回 false；FormatMetricName 4：lowercase/dash/space/combined；SaveMetrics 4：empty/single/multiple/nil labels；GetAlertRules 2：默认 4 条/配置覆盖；EvaluateAlerts 5：disabled/fired/not triggered/silence period/auto-resolve；ResolveStaleAlerts 3：2h stale/30min not stale/already resolved；CleanupExpiredMetrics 3：by retention/zero retention/no expired；AckAlert 2：success/not exist；GetMetricHistory 4：default/filter by name/limit boundary/limit=0 corrected；GetActiveAlerts 1：only firing；sendNotification 4：no webhook/200 success/500 fail/unreachable；SendAlertNotification 1：not exist；IsAlertEnabled/GetCollectInterval 3；常量 5：ConfigKeys/MetricNames/Severity/Status/Operator；Concurrent 1：5 goroutine CollectAndEvaluate no panic；CollectSystemMetrics 1：≥2 metrics；CollectAndEvaluate 集成 1；边界 4：negative/zero/empty/multiple metrics trigger）；15 个测试包全绿无回归，`go vet ./...` + `go build ./...` 通过）
- ✅ 通知系统（v0.4.0 第十一项迁移）：migration 014 新建 `notify_template` 表（id/tenant_id/code/channel/subject/body_html/status/created_at/updated_at）+ `notify_log` 表（id/tenant_id/channel/template_code/recipient/subject/body/content_length/provider_msg_id/status/error_message/retry_count/max_retry/priority/created_at + 4 索引）+ 16 项 notify.* sys_config（`notify.sms.enabled`/`provider`=aliyun/`access_key_id`/`access_secret_enc`/`sign_name` + `notify.email.enabled`/`smtp_host`/`smtp_port`=465/`smtp_username`/`smtp_password_enc`/`from_address`/`from_name` + `notify.inapp.enabled`=1 + `notify.retry.times`=3 + `notify.retry.interval_seconds`=60 + `notify.rate_limit.per_minute`=60）；新建 `internal/notify` 包：`Render` 模板变量替换用 `strings.NewReplacer` 安全实现（铁律 06 防 SSTI，不用 `text/template`）+ `Manager.GetTemplate` 按 code+channel+tenant_id 查询（优先租户自定义 → 回退平台通用 tenant_id=0）+ `Manager.ListTemplates`/`CreateTemplate`/`UpdateTemplate`/`DeleteTemplate` 模板 CRUD + `Manager.IsChannelEnabled` 通道开关 + `Manager.CheckRateLimit` 单租户每分钟限流 + `Manager.Send` 同步发送流程（通道开关→限流→接收人校验→查模板→渲染→调 provider→写日志）+ `Manager.Retry` 失败重试 + `Manager.GetStats` 统计（修复 GORM Where 累积污染 bug：用 `baseWhere` 闭包新会话每次 Count）+ `Manager.ListLogs` 日志查询 + `Manager.SendByTemplateCode` 按模板代码发送入口；`SMSProvider` 接口 + `aliyunSMSProvider` 骨架实现（AccessKey 未配置时返回 `ErrProviderNotConfig`，生产环境接入阿里云 SDK 后补全签名）；`EmailProvider` 接口 + `smtpEmailProvider` 真实现（`net/smtp.SendMail` 发送 + AES-256-GCM 解密 SMTP 密码 + From 地址校验）；常量 5 个 TemplateCode + 3 个 Channel + 3 个 TemplateStatus + 3 个 LogStatus + 3 个 Priority + 16 个 CfgKey + 7 个错误；类型 `SendRequest`/`SendResult`/`Stats`；新建 `internal/handler/notify.go`：9 个 admin 端点（`AdminNotifyStatus` GET /admin/notify/status 通道开关 + 配置 + 24h 统计 + 待重试数 + `AdminListNotifyTemplates` GET /admin/notify/templates 分页 + channel 筛选 + `AdminCreateNotifyTemplate` POST + `AdminUpdateNotifyTemplate` PUT /admin/notify/templates/:id + `AdminDeleteNotifyTemplate` DELETE + `AdminListNotifyLogs` GET /admin/notify/logs 分页 + channel/status 筛选 + `AdminGetNotifyLog` GET /admin/notify/logs/:id + `AdminRetryNotifyLog` POST /admin/notify/logs/:id/retry 仅 failed 可重试 + `AdminTestNotify` POST /admin/notify/test 自定义收件人 + 渠道 + 模板代码测试发送）；router 注册 9 条新路由（全部 adminAuth + RecordOperation 审计）；`internal/notify/notify_test.go` 36 个测试全 PASS（Render 5：基本/未提供变量保留占位符/空 vars/特殊字符/嵌套 JSON 字符串；ValidateChannel 2；ParseVariables 3；GenerateVerifyCode 3：6 位默认/自定义长度/最小长度；IsChannelEnabled 3：sms/email/inapp 默认；CheckRateLimit 3：未超限/超限/limit=0 不限流；GetTemplate 4：租户自定义/回退平台通用/未找到/disabled；Send 7：通道禁用/限流/空 recipient/模板未找到/sms 成功/email 成功/inapp 成功；Retry 3：非 failed 拒绝/sms 成功/email 成功；GetStats 2：全部状态统计/tenant 隔离；TemplateCRUD 1；ListLogs 1；TestDispatch 1；常量 1）；测试过程中发现并修复了 2 个生产 bug：① GORM 列名 `provider_msg_id` ≠ JSON tag `provider_msgid` 的 snake_case 转换差异（Updates map 必须用 GORM 列名，不能用 JSON tag）② GetStats 中复用同一 `*gorm.DB` 查询链导致 Where 条件累积（`status=sent AND status=failed` 永真为空 → 0），改用 `baseWhere` 闭包每次新建会话；15 个测试包全绿无回归，`go vet ./...` + `go build ./...` 通过）
- ✅ 终端用户体系（v0.4.0 第十二项迁移）：migration 015 新建 `end_user` 表（id/tenant_id/username/email/phone/password_hash/status/last_login_at/last_login_ip/register_ip/avatar/created_at/updated_at + 3 索引 unique(tenant_id,username)/unique(tenant_id,email)/index(status)) + `end_user_card` 表（id/user_id/card_id/tenant_id/bound_at + unique(user_id,card_id) + index(tenant_id,user_id)) + `end_user_token` 表（id/user_id/refresh_token_hash/access_jti/expires_at/user_agent/ip/revoked/created_at + index(user_id) + index(access_jti)) + ALTER `app_card` ADD `end_user_id` BIGINT NULL + 10 项 enduser.* sys_config（`enduser.register_enabled`=1 / `enduser.login_enabled`=1 / `enduser.password_min_length`=8 / `enduser.username_min_length`=3 / `enduser.refresh_token_ttl_days`=30 / `enduser.access_token_ttl_minutes`=120 / `enduser.max_sessions_per_user`=10 / `enduser.verify_code_ttl_minutes`=5 / `enduser.bcrypt_cost`=12 / `enduser.default_avatar`）；新建 `internal/enduser` 包：`Manager.Register` 注册（用户名/邮箱唯一性校验 + bcrypt cost=12 哈希 + 状态 active）+ `Manager.Login` 登录（账号查找 + bcrypt 校验 + 单租户会话上限校验 + 创建 access+refresh token + 异步更新 last_login_at/ip）+ `Manager.ValidateAccessToken` access token 校验（HMAC-SHA256 重新签名比对 + `hmac.Equal` 常量时间比较防时序攻击 + exp 过期校验 + jti 黑名单校验）+ `Manager.RefreshToken` refresh token 轮换（SHA-512 哈希比对 + 旧 token 撤销 + 创建新对）+ `Manager.Logout`/`RevokeSession`/`RevokeAllSessions` 会话管理 + `Manager.ListSessions` 当前会话列表 + `Manager.BindCard` 绑卡（事务化：`FOR UPDATE` 锁卡 → 校验 status=unused + app_card.end_user_id IS NULL → 写 end_user_card + 更新 app_card.end_user_id）+ `Manager.UnbindCard` 解绑（事务化反向操作）+ `Manager.ListMyCards`/`GetCardDetail` 卡密查询 + `Manager.GetProfile`/`UpdateProfile`/`ChangePassword`/`ResetPassword` 用户管理；access token 用简化格式 `base64(HMAC-SHA256(secret|user_id|app_id|exp).signature)`（避免在 enduser 包引入 jwt 依赖）+ refresh token 用 UUID + SHA-512 哈希存储（DB 不存明文）+ jti 单点踢出（写入 end_user_token.access_jti + 黑名单 Redis key `enduser:access:blacklist:jti:{jti}`）；新建 `internal/middleware/auth.go` 新增 `H5EndUserAuth(secret)` 中间件：从 Authorization: Bearer 头解析 access token + 调用 `enduser.Manager.ValidateAccessToken` + `hmac.Equal` 常量时间比较 + 注入 `user_id`/`tenant_id`/`jti` 到 gin.Context；新建 `internal/handler/enduser.go`：19 个端点（5 公开：`H5EndUserRegister` POST /api/v1/public/enduser/register + `H5EndUserLogin` POST /api/v1/public/enduser/login + `H5RefreshToken` POST /api/v1/public/enduser/refresh + `H5SendVerifyCode` POST /api/v1/public/enduser/verify_code + `H5ResetPassword` POST /api/v1/public/enduser/reset_password；10 H5（H5EndUserAuth 鉴权）：`H5EndUserMe` GET /api/v1/h5/me + `H5EndUserUpdateProfile` PUT /api/v1/h5/me + `H5EndUserChangePassword` POST /api/v1/h5/me/password + `H5EndUserLogout` POST /api/v1/h5/logout + `H5EndUserListSessions` GET /api/v1/h5/sessions + `H5EndUserKickSession` POST /api/v1/h5/sessions/:jti/kick + `H5EndUserBindCard` POST /api/v1/h5/cards/bind + `H5EndUserUnbindCard` POST /api/v1/h5/cards/unbind + `H5EndUserListMyCards` GET /api/v1/h5/cards + `H5EndUserGetCardDetail` GET /api/v1/h5/cards/:id；4 admin：`AdminListEndUsers` GET /api/v1/admin/endusers + `AdminEndUserStats` GET /api/v1/admin/endusers/stats + `AdminGetEndUser` GET /api/v1/admin/endusers/:id + `AdminUpdateEndUserStatus` PUT /api/v1/admin/endusers/:id/status）；router 注册 19 条新路由（5 publicGroup + 10 h5Auth 新分组 + 4 adminAuth）+ `h5Auth := v1.Group("/h5")` 新分组挂载 `middleware.H5EndUserAuth`；`internal/enduser/enduser_test.go` 53 个测试全 PASS（Register 6：正常/用户名已存在/邮箱已存在/用户名过短/密码过短/注册关闭；Login 4：账号不存在/密码错误/正常/登录关闭；ValidateAccessToken 4：有效/过期/篡改/jti 黑名单；parseUA 4：Chrome/Firefox/空 UA/curl；RefreshToken 3：有效/无效/已撤销；Logout/Revoke 4：Logout 单会话/RevokeSession 按 jti/RevokeAllSessions/无效 jti；BindCard 8：正常/卡不存在/卡已用/卡已绑他人/已绑自己/事务回滚验证/卡 banned/卡 expired；UnbindCard 2：正常/未绑定；ListMyCards/GetCardDetail 4：列表/详情/不存在/越权；UpdateProfile 2：正常/邮箱冲突；ChangePassword 3：旧密码错/正常/同密码；ResetPassword 2：正常/验证码错；Helpers 4：generateAccessToken/parseAccessToken/generateRefreshToken/hashRefreshToken；Boundary 3：max_sessions=0 不限/max_sessions=1 单会话/并发登录淘汰最旧）；15 个测试包全绿无回归，`go vet ./...` + `go build ./...` 通过）
- ✅ API 开放平台（v0.4.0 第十三项迁移）：migration 016 新建 `developer_api_token` 表（id/tenant_id/name/token_hash/prefix/scopes/expires_at/last_used_at/last_used_ip/status/revoked_at/created_at + 3 索引 unique(token_hash)/index(tenant_id,status)/index(prefix)) + `webhook_endpoint` 表（id/tenant_id/name/url/secret_enc/events/status/failure_count/last_response_code/last_response_at/last_error/created_at/updated_at + 3 索引 index(tenant_id,status)/index(status)/index(tenant_id,url)) + `webhook_delivery` 表（id/tenant_id/endpoint_id/event_type/event_id/payload/status/http_status/response_body/attempt_count/max_retry/next_retry_at/delivered_at/created_at + 5 索引 index(tenant_id,event_type)/index(endpoint_id,status)/index(status)/index(next_retry_at)/index(event_id)) + 8 项 openapi.*/webhook.* sys_config（`openapi.token_ttl_days`=365 / `openapi.token_max_per_tenant`=20 / `openapi.token_name_max_length`=64 / `openapi.request_timeout_seconds`=30 / `webhook.retry.max_attempts`=5 / `webhook.retry.backoff_seconds`=120 / `webhook.endpoint.disable_threshold`=10 / `webhook.delivery.retention_days`=30）；新建 `internal/openapi` 包：`TokenManager` 4 方法 `GenerateToken`（`crypto/rand` 生成 32 字节随机 → base62 编码 → `prefix_xxxxxxxx` 形式 → SHA-512 哈希存储 token_hash + 前 8 位明文 prefix 用于展示识别 + 同租户数量上限校验 + 可选 TTL）+ `ValidateToken`（SHA-512 哈希后 DB 查找 + status=active + revoked_at IS NULL + expires_at 校验 + 异步更新 last_used_at/last_used_ip）+ `RevokeToken`（标记 status=revoked + revoked_at）+ `ListTokens`/`GetToken`；`WebhookManager` 6 方法 `CreateEndpoint`（URL https 强制 + events 非空 + AES-256-GCM 加密 secret 入库 secret_enc）+ `UpdateEndpoint`/`DeleteEndpoint`/`GetEndpoint`/`ListEndpoints` + `DispatchEvent`（按 event_type 查询订阅的 active endpoints → 异步分发到每个 endpoint：写 pending delivery → HMAC-SHA256(secret, event_id|timestamp|payload) 签名 + `hmac.Equal` 常量时间比较防时序攻击 + HTTP POST + 2xx 成功标记 delivered + 非 2xx 触发退避重试 next_retry_at = now + attempt * backoff_seconds + 连续失败 failure_count++，达 disable_threshold 自动 status=disabled）+ `RetryDelivery`（重新分发指定 delivery）+ `ProcessPendingRetries`（定时扫描 next_retry_at <= now 的 pending delivery 重试，达 max_attempts 标记 failed）；8 个预定义 Scope 常量（card.read/write + order.read/write + agent.read/write + webhook.read/write）+ `HasScope` 工具 + `ParseScopes`/`FormatScopes` 转换 + 5 个事件类型常量（`EventOrderPaid`/`EventCardGenerated`/`EventAgentRegistered`/`EventAgentRechargeApproved`/`EventAgentWithdrawPaid`）；类型 `DeveloperAPIToken`/`WebhookEndpoint`/`WebhookDelivery`/`TokenCreateResult`/`DeliveryResult`；新建 `internal/middleware/auth.go` 新增 `APITokenAuth(mgr)` 中间件：从 Authorization: Bearer 头解析 token + 调用 `TokenManager.ValidateToken` + 按 error 类型映射错误码（`ErrTokenRevoked`/`ErrTokenExpired`/`ErrTokenNotFound`）+ 注入 `api_token_id`/`api_tenant_id`/`api_scopes`/`api_token_name` 到 gin.Context + `RequireScope(scopes...)` 中间件：基于 `HasScope` 校验任一 scope 命中即放行；新建 `internal/handler/openapi.go`：15 个端点（1 admin：`AdminOpenAPIStatus` GET /admin/openapi/status 平台总览统计 + 13 tenant：Token CRUD 4 + Webhook endpoint CRUD 5 + Delivery 列表/详情/重试 3 + Meta 元信息 1 + 1 openapi：`OpenAPIWhoami` GET /api/v1/openapi/whoami 第三方调用方自校验）+ `DispatchWebhookEvent(deps, tenantID, eventType, payload)` 异步分发辅助函数（goroutine + panic recover + context.Background + best-effort，供 5 个业务点接入）；5 个业务点接入 Webhook 事件分发：`card.go` `TenantGenerateCards` 成功后 card.generated（仅批次级元信息，不含卡密明文）+ `pay.go` `processPaidOrder` 成功后 order.paid + `pay.go` `processAgentRegisterPaid` 成功后 agent.registered（事务外捕获 agentID 防止 tx 内 ID 失真）+ `tenant_finance.go` `TenantApproveRecharge` 成功后 agent.recharge.approved + `tenant_finance.go` `TenantPayWithdraw` 成功后 agent.withdraw.paid；router 注册 15 条新路由（adminAuth 1 + tenantAuth 13 + 新增 openapiAuth 路由组挂载 `middleware.APITokenAuth` 1）；`internal/openapi/openapi_test.go` 61 个测试全 PASS（TokenManager GenerateToken 6：正常/无 TTL/数量超限/名称超长/空名称/rand 失败 mock；ValidateToken 8：有效/已撤销/已过期/不存在/last_used 异步更新/空 token/IP 记录/边界；RevokeToken 3：成功/不存在/已撤销；ListTokens/GetToken 4：列表/分页/详情/越权；WebhookManager CreateEndpoint 5：正常/URL 非 https 拒绝/events 空拒绝/AES 加密验证/重复 URL；UpdateEndpoint 3：成功/不存在/越权；DeleteEndpoint 2：成功/不存在；ListEndpoints 2：列表/筛选；DispatchEvent 8：无订阅/单 endpoint/多 endpoint/endpoint disabled 跳过/HTTP 失败退避/连续失败阈值 disable/HMAC 签名验证/异步分发不阻塞；RetryDelivery 3：成功/达上限/不存在；ProcessPendingRetries 3：扫描重试/达 max_attempts 标记 failed/无 pending；Scope 工具 4：HasScope 命中/未命中/空 scopes/ParseScopes 边界；常量 4：8 个 scope 互不冲突/5 个事件类型互不冲突/配置键前缀；HMAC-SHA256 签名验证 2：签名生成正确/篡改 payload 拒绝；AES-256-GCM 加解密 2：secret 加密 round-trip/不同 tenant 隔离；hex 解码 2：SHA-512 hash 长度一致/不同输入 hash 不同）；16 个测试包全绿无回归，`go vet ./...` + `go build ./...` 通过；详见 SPEC 7.4 API 开放平台规范）
- ✅ 管理员更新弹窗通知（v0.4.0 第十四项迁移）：migration 017 新增 2 项 `update.poll.*` sys_config（`update.poll.enabled`=1 弹窗通知总开关 1=启用前端 AdminLayout 30s 轮询 / 0=关闭弹窗通知 / `update.poll.interval_seconds`=30 轮询间隔秒数 最小 10 秒由后端 `PollIntervalMin` 常量强制下限防配置错误导致前端打爆后端）；`internal/update/update.go` 新增 2 个配置键常量 `CfgKeyPollEnabled`/`CfgKeyPollInterval`（铁律 04：禁止硬编码）+ `PollIntervalMin=10` 下限常量；新建 `AdminUpdatePoll(deps)` handler GET `/admin/update/poll`（adminAuth 鉴权）轻量轮询端点：仅返回 8 字段 `enabled` + `interval_seconds` + `current_commit` + `is_locked` + `last_update_at` + `last_status` + `last_trigger` + `last_commit`（不含 `log_text`/`steps_json` 重字段，降低高频轮询带宽）+ 间隔下限保护（从 sys_config 读取后若 < `PollIntervalMin` 强制提升到 10 秒）+ 配置即时生效（每次轮询都重新读取 sys_config，后端调整开关/间隔下一次轮询立即生效）；router 注册 1 条新路由（adminAuth 组 `GET /admin/update/poll`）；前端 `apps/admin/src/api/update.ts`（新建）补全 6 个 API：`updateStatusApi`/`triggerUpdateApi`/`listUpdateHistoryApi`/`getUpdateLogApi`/`rollbackUpdateApi` + 新增 `pollUpdateApi` + `UpdatePoll` 类型；前端 `apps/admin/src/components/UpdateNotifier.vue`（新建）无 UI 仅逻辑组件，挂载于 `AdminLayout.vue`：localStorage key `keyauth_admin_last_known_commit` 持久化上次已知 commit 跨会话检测更新 + `pollOnce()` 异步函数调 `pollUpdateApi` 返回后端建议间隔（秒），`enabled=false` 返回 0 信号停止定时器，异常返回 30 兜底 + `scheduleNext(intervalSec)` 自适应定时器（每次轮询后用响应中的 `interval_seconds` 重置 setInterval，后端调整配置即时生效）+ `showRefreshDialog(newCommit)` 使用 `ElMessageBox.confirm` 弹窗「立即刷新」/「稍后提醒」 + `notifiedCommit` ref 防本会话重复弹窗（用户选「稍后提醒」后本会话不再打扰）+ 用户选「立即刷新」→ `window.location.reload()` 强制重新加载所有资源 + `onMounted` 启动轮询 `onBeforeUnmount` 停止定时器 + 强制下限 10 秒与后端 `PollIntervalMin` 对齐；前端 `apps/admin/src/layouts/AdminLayout.vue` 挂载 `<UpdateNotifier />` 组件（与 `<BasicLayout>` 同级，无 UI 不影响布局）；`internal/handler/update_poll_test.go` 13 个测试全 PASS（默认配置 enabled=true + interval=30 / 有审计日志时返回最近一次更新元信息 / 多条日志时返回最新一条 / 配置 enabled=0 时返回 false / 自定义间隔 60 生效 / 低于下限 5 强制提升到 10 / 等于下限 10 保留 / 空表时 last_* 全部 nil / 响应不含 log_text/steps_json 重字段 / 回滚状态正确返回 / 配置动态变更即时生效 / 配置键常量正确 / 响应包含所有预期字段）；测试运行在 git 仓库内时 `current_commit` 返回真实 commit hash，断言改为仅验证类型为 string 避免依赖运行环境；16 个测试包全绿无回归 + `vue-tsc --noEmit` 前端 TypeScript 检查通过，`go vet ./...` + `go build ./...` 通过；详见 SPEC 7.4.3 在线更新体系规范 + CHANGELOG v0.4.0 第十四项迁移）
- ✅ 高级安全（v0.4.0 第十五项迁移）：migration 018 新建 3 张新表 `risk_rule`（id/name/rule_type/condition/score/action/priority/status/created_by + 4 索引）+ `risk_event`（id/user_type/user_id/username/rule_id/rule_type/risk_score/action_taken/detail/client_ip/user_agent/occurred_at/acknowledged/acknowledged_by/acknowledged_at + 6 索引）+ `login_geo_alert`（id/user_type/user_id/username/current_ip/current_network/previous_ip/previous_network/alert_status/notify_channels/notify_sent/detail/acknowledged_by/acknowledged_at/closed_at/created_at + 4 索引）+ ALTER `app_device` 增加 6 字段 `hwid_components`/`user_agent`/`client_ip_ext`/`screen_resolution`/`timezone`/`language`（v0.4.0 设备指纹多维度升级，向前兼容）+ 16 项 `cloudflare.*`/`risk.*` sys_config（`cloudflare.enabled`=0 / `cloudflare.real_ip_header`=`CF-Connecting-IP` / `cloudflare.ip_country_header`=`CF-IPCountry` / `cloudflare.trusted_cidrs`=Cloudflare 官方 IPv4/IPv6 CIDR 列表 + `risk.engine.enabled`=1 / `risk.engine.block_threshold`=100 / `risk.engine.challenge_threshold`=50 + `risk.geo_login_alert.enabled`=1 / `risk.geo_login_alert.ipv4_prefix`=24 / `risk.geo_login_alert.ipv6_prefix`=64 / `risk.geo_login_alert.notify_channels`=`inapp,email` + `risk.new_device_score`=40 / `risk.abnormal_ua_score`=30 / `risk.abnormal_time_start`=`02:00` / `risk.abnormal_time_end`=`06:00` / `risk.high_frequency_threshold`=10）+ 5 条 seed 内置规则（异地登录 geo_login 60 分 alert / 新设备 new_device 40 分 alert / 异常 UA abnormal_ua 30 分 alert / 异常时段 abnormal_time 20 分 disabled 默认禁用 / 高频请求 high_frequency 50 分 challenge，全部 created_by=system 禁删禁改类型）；新建 `internal/risk/risk.go`（901 行）：16 个配置键常量（铁律 04：禁止硬编码）+ 6 个规则类型常量 + 3 个动作常量 + 2 个状态常量 + 4 个用户类型常量 + `ConfigReader` 接口（与 middleware.ConfigReader 兼容）+ `EvalContext` 评估上下文（UserType/UserID/Username/ClientIP/UserAgent/HWID/Operation/OccurredAt）+ `EngineOutput` 引擎输出（TotalScore/Action/HitRules/ShouldBlock/ShouldChallenge）+ `Manager.EvaluateLogin(ctx, ec)` 主入口（遍历 active 规则 → 评估 → 累计评分 → 阈值升级动作 block_threshold=100 / challenge_threshold=50）+ `Manager.RecordEvent(ctx, ec, out)` 落盘（写 risk_event + 异地登录额外写 login_geo_alert）+ 5 条内置规则评估函数：`evalGeoLogin`（IP 网段比较 IPv4 /24 IPv6 /64，查 RefreshTokenDevice 表上次登录 IP，无需 GeoIP 数据库）+ `evalNewDevice`（UA 比对近似，检查 RefreshTokenDevice 表历史 UA）+ `evalAbnormalUA`（curl/wget/python-requests/bot 关键词 + pkg/ua Bot 识别）+ `evalAbnormalTime`（HH:MM 范围判断支持跨午夜）+ `evalHighFrequency`（统计 risk_event 表近期事件数）+ `NetworkOf(ipStr, ipv4Prefix, ipv6Prefix)` 工具函数计算 IP 网段 CIDR + CRUD：`ListRules`/`GetRule`/`CreateRule`（仅 custom 类型，默认 CreatedBy=admin 防 DeleteRule 误判为内置）/`UpdateRule`（内置不可改 rule_type）/`DeleteRule`（内置不可删，判断 CreatedBy=system）+ 事件/告警查询：`ListEvents`+`AcknowledgeEvent` / `ListGeoAlerts`+`AcknowledgeGeoAlert`+`CloseGeoAlert` + `GetStats` 风控看板（今日/本周事件数 + 各动作计数 + 待处理告警 + TOP 10 异常 IP + 最近 10 条事件）；新建 `internal/middleware/cloudflare.go`（150 行）：`CloudflareRealIP(cfgReader)` 中间件（`cloudflare.enabled=0` 时直接回退 c.ClientIP() / `cloudflare.enabled=1` 时从配置头名取真实 IP 默认 CF-Connecting-IP + 校验来源 IP 在 `cloudflare.trusted_cidrs` 列表内 + 注入 real_ip + ip_country 到 gin.Context，校验失败回退 c.ClientIP() 确保非 CF 部署环境也能正常工作）+ `RealIP(c)` 工具函数（优先取 ContextKeyRealIP 回退 c.ClientIP()）+ `IPCountry(c)` 工具函数 + `ipInCIDRList`/`hostFromAddr` 辅助函数；新建 `internal/middleware/risk_engine.go`（55 行）：`RiskEngineForAnonymous(mgr)` 中间件对匿名请求做风控评估（不阻塞流程，仅记录命中规则），命中 block 时返回 403 + code 1006「请求已被风控引擎拦截」；修改 `internal/middleware/ratelimit.go`：`RateLimitByIP`/`IPBlacklist` 中 `c.ClientIP()` → `RealIP(c)`（共 3 处变更），确保 Cloudflare 部署环境下 IP 限流和黑名单基于真实客户端 IP；修改 `internal/handler/deps.go`：`Deps` 新增 `RiskMgr *risk.Manager` 字段（nil = 禁用风控）；修改 `internal/handler/auth.go`：步骤 7.1（写入会话记录）之后、步骤 8（清除失败计数）之前插入步骤 7.2 风控评估（调用 `deps.RiskMgr.EvaluateLogin(ctx, ec)` 评估，`out.ShouldBlock` 时撤销会话 revokeSessionByJTI + 清除失败计数 + 记录事件 + 返回 403「登录已被风控引擎拦截」，`len(out.HitRules) > 0` 时记录事件不阻塞登录）；新建 `internal/handler/risk.go`（388 行）：admin 风控面板 11 个端点（`AdminRiskStats` GET /admin/security/risk/stats 风控看板统计 + `AdminListRiskRules`/`AdminCreateRiskRule`/`AdminGetRiskRule`/`AdminUpdateRiskRule`/`AdminDeleteRiskRule` 5 个规则 CRUD + `AdminListRiskEvents`/`AdminAckRiskEvent` 2 个事件管理 + `AdminListGeoAlerts`/`AdminAckGeoAlert`/`AdminCloseGeoAlert` 3 个异地告警管理）；修改 `internal/router/router.go`：import 添加 `internal/risk` + 全局中间件注册 `CloudflareRealIP`（在 IPBlacklist 之前）+ Deps 注入 `RiskMgr: risk.NewManager(container.DB, container.ConfigCache())` + `adminAuth` 组注册 11 条新路由；`internal/risk/risk_test.go` ~30 个测试全 PASS（NetworkOf 6：IPv4/IPv6/无效 IP/同网段/不同网段/IPv4 边界；parseHHMM 2：有效/无效；actionLevel 1：alert<challenge<block；异常 UA 4：空 UA/curl/python-requests/正常浏览器；异常时段 3：范围内/范围外/跨午夜；异地登录 4：无历史/同网段/不同网段/同 IP；新设备 3：首次登录/同 UA/不同 UA；高频请求 2：低于阈值/达到阈值；EvaluateLogin 2：引擎禁用/命中多规则；RecordEvent 1：异地登录告警落盘；规则 CRUD 5：内置禁止创建/自定义创建/内置禁止删除/自定义删除/内置不可改类型；事件/告警确认 3：AcknowledgeEvent/AcknowledgeGeoAlert/CloseGeoAlert；统计 2：空表/有事件；常量 3：配置键/规则类型/动作）+ `internal/middleware/cloudflare_test.go` 5 个测试全 PASS（CF 禁用回退 c.ClientIP / CF 启用从 CF-Connecting-IP 取真实 IP + CF-IPCountry 取国家 / 受信 CIDR 校验通过 / 受信 CIDR 校验失败回退 / 自定义头名 X-Real-IP）；17 个测试包全绿无回归，`go vet ./...` + `go build ./...` 通过；详见 SPEC 6.4 防御措施新增 v0.4.0 异常登录/异地登录/真实 IP 伪造 3 行 + CHANGELOG v0.4.0 第十五项迁移；铁律遵循：铁律 04 全部常量化 / 铁律 05 16 项配置走 sys_config 后台可视化编辑实时生效 / 铁律 06 所有测试基于固定输入断言无随机性 + 异地登录检测基于 IP 网段比较无需 GeoIP 数据库 + CF 中间件 enabled=0 时直接回退 c.ClientIP())

v0.3.5 已完成（基线）：
- ✅ 后端 Go 项目结构（main / config / model / middleware / handler / router / quota / migration / heartbeat）
- ✅ 26 张基础表 + 4 张扩展表（log_login_failed / refresh_token_device / tenant_balance_log / tenant_withdraw）共 30 个 GORM struct，7 套 migration（001~007）
- ✅ 后端业务 API 全量实现：17 个 handler 文件，143 条路由，三角色 dashboard/profile/CRUD/2FA/登录设备全部真实实现，无 501 占位
- ✅ 三角色前端：45 个 .vue 页面，BasicLayout + AdminLayout/TenantLayout/AgentLayout + 官网 + H5，全部响应式
- ✅ 平台总支付闭环（彩虹易支付：下单/回调/同步跳转/自动发卡/防重入/超时关闭/抽成结算）
- ✅ 代理体系闭环（邀请码 + 充值审核 + 余额扣款生成卡密 + 佣金计算 + 提现审核 + 三级通知）
- ✅ 开发者结算与对账闭环（balance/frozen_balance + 批量结算 + 对账报表 + 双审核页面）
- ✅ 日志系统（验证/操作异步 worker + 三表独立查询 + UTF-8 BOM CSV 导出）
- ✅ 套餐配额统一封装（quota 包：CheckMaxApps/MaxCards/MaxAgents/MaxDevices）
- ✅ 轻量级 SQL 文件迁移机制（schema_migrations 表 + dirty 状态 + 单迁移事务）
- ✅ H5 公共 API（PublicAppInfo + PublicCardTypes，购卡闭环）
- ✅ Docker Compose + 宝塔部署 + RSA-4096 密钥生成独立脚本

**v0.3.6 已完成（2026-07-20）**：剩余 P1 收尾 + 单元测试 + 客户端 SDK 签名对齐测试，全部完成。

**v0.4.0 进行中（2026-07-20）**：UA 解析迁移 + JWT jti 精准单点踢出 + 2FA backup_codes DB 持久化 + 登录失败日志结构化 slog + 全语言 SDK 扩展 + 多级代理体系 + 灰度发布体系 + 在线更新体系 + 数据备份恢复体系 + 监控告警体系 + 通知系统 + 终端用户体系 + API 开放平台 + 管理员更新弹窗通知 + 高级安全 15 项迁移全绿（17 个测试包全 PASS；新增 multilevel 包 27 个测试 + grayscale 包 33 个测试 + update 包 37 个测试 + backup 包测试 + monitor 包 53 个测试 + notify 包 36 个测试 + enduser 包 53 个测试 + openapi 包 61 个测试 + update_poll_test 13 个测试 + risk 包 ~30 个测试 + cloudflare_test 5 个测试共 380+ 个新测试），后续推进前端 H5 接入等。

**v0.4.0（三期商业化，已完成）**：
- ✅ 通知系统 + 终端用户体系 + API 开放平台 + 管理员更新弹窗通知 + 高级安全

详细任务见 `docs/TODO.md`。

## 六、开发工作流

1. **接到新需求时**：先阅读 `docs/TODO.md`，确认优先级与依赖
2. **写代码前**：阅读对应铁律 + SPEC 规范，确认涉及哪些表与配置
3. **写代码时**：所有可调参数通过 `cfgCache.GetString("key", "默认值")` 读取
4. **写完后**：
   - 同步更新 `docs/CHANGELOG.md` 与 `docs/TODO.md`
   - 涉及新表/字段：补 migration SQL
   - 涉及新配置项：在 `002_seed_data.up.sql` 中追加默认值
   - 涉及新接口：在 SPEC.md 中补 API 文档
5. **提交前**：检查铁律合规性（grep 是否有硬编码 / 是否有待核实标注）

## 七、关键配置键速查

| 业务场景 | 配置键 | 默认值 |
|---|---|---|
| 心跳间隔 | `verify.heartbeat.interval` | 60 |
| IP 全局限流 | `security.rate.limit_global` | 100 |
| IP 自动封禁阈值 | `security.ban.threshold` | 50 |
| 平台易支付网关 | `pay.platform.gateway_url` | （待填写） |
| 平台抽成比例 | `pay.platform.commission_default` | 5.00 |
| 代理注册费 | `agent.register.fee` | 99.00 |
| 最低提现金额 | `agent.min_withdraw_amount` | 100.00 |
| 平台公告横幅开关 | `notice.platform.banner_enabled` | 1 |

完整列表见 `apps/server/migrations/002_seed_data.up.sql`。

## 八、调试技巧

**本地启动后端**：
```bash
cd apps/server
cp ../../configs/config.yaml.example ../../configs/config.yaml
# 修改 configs/config.yaml 中 mysql/redis 指向本地
go run ./cmd --config=../../configs/config.yaml
```

**本地启动前端**：
```bash
cd apps/admin
npm install
npm run dev   # 默认 http://localhost:5173
```

**查看 SQL 日志**：MySQL 配置中开启了慢查询日志（>2s），通过 `docker logs keyauth-mysql` 查看。

**重置超管密码**：
```bash
bash scripts/reset_admin_password.sh NewPass@2026
```

## 九、可信度声明

本项目中以下内容已标注「待核实」（铁律 06）：
- `pkg/crypto/crypto.go` 中 HMAC-SHA256 使用 `sha512.New512_256` 变体，需验证与客户端 SDK 一致性
- `pkg/snowflake/snowflake.go` 中 `twepoch = 1767225600000`（2026-01-01 UTC）
- `migrations/002_seed_data.up.sql` 中默认超管密码哈希为占位，部署后通过 `/install` 向导重置（v0.3.6 替代原"占位 hash + 后置脚本"方案）
- `scripts/reset_admin_password.sh` 中后端 `--reset-admin-password` subcommand 需在 main.go 实现

> v0.3.6 已修复：原 `card.go:422` 设备强制下线 TODO 已实现（联动 heartbeat.Remove）；卡密 CSV 导入导出已实现；原 `auth.go:443` AgentRegister 501 占位已实现（方案 B 先支付后建 Agent）；Register.vue 三处 TODO 已落地（读配置+调起支付+查询订单状态）；install.go 配置键名 bug 已修复（`agent.register_fee` → `agent.register.fee` 与 seed 002 对齐）；原 `pay.go:528` `EpayTenantNotify` 占位 `c.String(200, "fail")` 已实现完整回调流程（含 `processTenantOwnPaidOrder` 事务 + `loadTenantPayConfig` AES 解密）；双层支付模式切换已生效（`CreatePayOrder` 内 `SysPackage.AllowCustomPay` + `TenantPayConfig.Enabled` 双开关，TOP/ORD 前缀分发）；`002_seed_data.up.sql` 中 `pay.platform.notify_path` 与 router 不一致 bug 已修复；客户端 SDK 三语言已发布（`sdks/python/` + `sdks/nodejs/` + `sdks/php/`，9 个验证 API + HMAC-SHA512/256 签名算法 + KeyAuthError 异常体系，PHP `php -l` 校验通过）。

> 客户端 SDK 签名算法「待核实」项更新（v0.3.6 已通过单元测试部分验证）：三语言 SDK 优先用 `sha512/256` 算法（与后端 `crypto.HMACSHA256` 的 `sha512.New512_256` 变体对齐），运行时不支持时回退标准 `sha256`。**已验证**：Python + PHP 在 sha512/256 可用环境下与后端签名完全一致（`sdks/tests/sign.py` + `sign.php` + `pkg/crypto/sign_alignment_test.go`，3 组固定输入全 PASS）；**待核实**：sha256 回退分支与 sha512.New512_256 不等价（已确认两者输出不同，回退会导致签名校验失败），Node.js 沙箱环境 OpenSSL 不支持 sha512/256，已 `t.Skipf` 标注「环境限制」，待生产环境 Node.js 验证。

> v0.4.0 已落地：UA 解析库迁移完成（`pkg/ua` 自实现零第三方依赖 + 20 个测试全 PASS；handler 层 `parseDeviceName` / `detectDeviceType` / `ListLoginDevicesFull` 全部接入；ListLoginDevices 响应新增 `os` / `os_version` / `browser` / `browser_version` / `is_bot` 字段，不改 DB schema 向前兼容）；profile.go 中原「待核实 v0.4.x：引入更完整的 UA 解析库」标注已移除。

> v0.4.0 已落地：JWT jti 精准单点踢出完成（`internal/auth` 包新增 `BlacklistRefreshTokenByJTI` + `IsRefreshTokenBlacklisted` 改造为双维度优先 jti；`internal/middleware/auth.go` `JWTAuth` 注入 `c.Set("jti", claims.ID)` + `GenerateToken` 保留 `claims.ID` 修复 jti 丢失 bug；`internal/handler/auth.go` Login/RegisterTenant/RefreshToken/Logout 全部生成并传递 jti；`internal/handler/session.go` `recordLoginSession` 增加 jti 参数 + 新增 `revokeSessionByJTI`；`internal/handler/profile.go` KickDevice 注释更新为「v0.4.0 已支持精准单点踢出」，ChangePassword/Disable2FA 故意保留 user 维度；18 个 auth 测试 + 1 个 middleware JTI 注入测试全 PASS，8 个测试包全绿；兼容旧 token：无 jti 回退 user 维度）；session.go 中原「待核实 v0.4.x：将 jti 嵌入 JWT 后改为只黑名单指定 jti」标注已移除。

> v0.4.0 已落地：2FA backup_codes DB 持久化完成（`migrations/008_v0.4.0_2fa_backup_codes.up.sql` 三表 sys_admin/sys_tenant/agent 加 `backup_codes VARCHAR(512) NOT NULL DEFAULT ''` 字段 + `internal/model/model.go` 三表 struct 同步加 `BackupCodes` 字段；`internal/handler/profile.go` 新增 `loadUserBackupCodes`/`updateUserBackupCodes`/`consumeBackupCode` 三函数，Verify2FA 第 4 步从 Redis 持久化改为 DB 字段写入 + 清理 Redis 老数据，Disable2FA 第 5 步清空 DB `backup_codes` 字段 + 清理 Redis；兼容 v0.3.x 老用户：`loadUserBackupCodes` DB 为空时回退 Redis 读取，`consumeBackupCode` 消费成功后回写 DB + 清理 Redis 老数据；`internal/handler/profile_2fa_test.go` 13 个测试全 PASS，覆盖 DB 读取 / Redis 回退 / 消费 / 边界 / role 分支 / 兼容路径全场景）；profile.go 中原「待核实 v0.4.x：备用码理想方案为 bcrypt 哈希入库，当前简化用 AES 加密存 Redis，后续 v0.4.x 加 backup_codes 字段后迁移」标注已移除；session.go 中原「待核实 v0.4.x：引入结构化日志记录此错误」标注已移除（log_worker.go 同步移除）。

> v0.4.0 已落地：登录失败日志结构化完成（新建 `internal/logger` 包基于 Go 1.21+ 标准库 `log/slog`，零第三方依赖取代 zap/zerolog；`Options{Level, Format, Output}` + `Init` 用 `atomic.Value` 并发安全切换 + `L()` / `Debug/Info/Warn/Error` + 4 个 `DebugCtx/InfoCtx/WarnCtx/ErrorCtx` 链路追踪版本；`internal/config/config.go` `AppConfig` 加 `LogLevel`/`LogFormat`/`LogOutput` 三个 yaml 字段；`cmd/main.go` 启动时调用 `logger.Init(logger.Options{...})` 从 config 注入；`internal/handler/session.go` `StartLoginFailureWorker` + `internal/handler/log_worker.go` `StartVerifyLogWorker` + `StartOperationLogWorker` 3 处 `_ = err` 静默丢弃替换为 `logger.Error("xxx write failed", "err", err, ...业务字段...)` 结构化日志；`internal/logger/logger_test.go` 6 个测试全 PASS：parseLevel 4 级别 + 大小写 + 默认值 / JSON 格式 level/msg/字段断言 / level=warn 过滤 debug+info / text 格式 msg 含空格自动加引号 / L() 非 nil / 空 Options 不 panic）。

> v0.4.0 已落地：全语言 SDK 扩展完成（`sdks/go/keyauth/keyauth.go` 用 `crypto/sha512.New512_256` 原生字节级对齐后端 + 9 个客户端 API 强类型 struct 返回 + 零第三方依赖；`sdks/java/src/main/java/com/keyauth/sdk/KeyAuthClient.java` 用 JDK 11+ HttpClient + Jackson + `Mac.getInstance("HmacSHA512/256")`（JDK 17+，回退 `HmacSHA256`）+ Maven 工程 + `KeyAuthException(code, message, httpStatus)`；`sdks/csharp/src/KeyAuth/KeyAuthClient.cs` 用 .NET 6+ HttpClient + `SHA512_256Provider` 类反射探测 BouncyCastle（`Org.BouncyCastle.Crypto.Digests.Sha512_256Digest`）启用 SHA-512/256 否则回退 `HMACSHA256` + System.Text.Json + 9 个 async API 返回 `Task<JsonElement>`；`sdks/cpp/include/keyauth/keyauth.hpp` + `sdks/cpp/src/keyauth.cpp` 用 C++17 + libcurl + OpenSSL 1.1+ `EVP_sha512_256` 原生对齐（OpenSSL < 1.1 回退 `EVP_sha256` + stderr 警告）+ nlohmann/json + CMake FetchContent 自动下载 + `keyauth::KeyAuthException`；`sdks/epl/keyauth_sdk.e.txt` 易语言纯中文 API + 精易模块 v9.0+ 依赖 + HMAC-SHA256（易语言生态无 SHA-512/256 实现，与后端算法不同，仅在后端 crypto.go:165 回退场景下匹配，已明确标注 mismatch）；`pkg/crypto/sign_alignment_test.go` 从 3 语言扩展到 7 语言自动化：Python/Node/PHP/Go 走解释器模式（`go run`）+ C++ 走 `g++` 编译 + Java 走 JDK 11+ 单文件源码模式（`java Sign.java`）+ C# 走 `dotnet` 临时项目编译运行 + 易语言永久 `t.Skip`（Windows-only，CI 无法运行）；新增 `TestSignAlignment_NewLanguages` 元数据校验测试（5 个新 SDK 目录结构 + README + 入口文件存在性 + 签名函数实现，CI 友好无运行时依赖）；JDK 版本检测：仅 JDK 17+ 才断言签名匹配，否则 `t.Logf` 暴露 mismatch 而非 `t.Skip` 掩盖；C# 在 fallback 场景也用 `t.Logf` 而非 `t.Skip`；11 个测试包全绿，`go vet ./...` + `go build ./...` 通过）。**待核实**：易语言 SDK 仅在后端 `crypto.go:165` sha256 回退分支下匹配，生产环境后端使用 `sha512.New512_256` 时签名不一致，需在易语言客户端文档中明确告知用户后端必须配置 sha256 回退；C# / Java 在 fallback 场景下与后端 sha512.New512_256 也不等价，需用户安装 BouncyCastle（C#）或 JDK 17+（Java）才能保证签名匹配。

> v0.4.0 已落地：多级代理体系完成（migration 009 + `internal/multilevel` 包跨级佣金 + 27 个测试全 PASS，详见 CHANGELOG / SPEC 7.4.1）。

> v0.4.0 已落地：灰度发布体系完成（`migrations/010_v0.4.0_grayscale_release.up.sql` `app_version` 表新增 5 字段 `release_strategy`/`grayscale_rate`/`grayscale_platforms`/`grayscale_regions`/`grayscale_channels` + 复合索引 `idx_app_status_strategy` + 补齐 `tenant_id` 字段修复 001 init schema 遗漏 + 3 项 sys_config：`app.version.grayscale.enabled`=1 全局开关 / `default_rate`=10.00 默认比例 / `hash_salt`=keyauth-grayscale-v040 Hash 桶盐值；`internal/model/model.go` `AppVersion` struct 同步加 5 字段 + gorm 默认值标签；新建 `internal/grayscale` 包：`Match` 7 步过滤链（nil version → full 命中 → 全局禁用回退 full → 平台过滤 → 渠道过滤（空默认 stable）→ 地区过滤 → 比例 0/100/Hash 桶判定）+ `HashBucket` SHA-256(salt:appID:clientID) 取前 4 字节 little-endian uint32 % 100 返回 0-99 稳定桶号 + `ParseList` 逗号分隔解析器（trim + 转小写 + 返回非 nil 切片）+ `DefaultRate` 读取默认比例 + `IsEnabled` 读取全局开关；常量 `StrategyFull`/`StrategyGrayscale`/`StrategyCanary` + 3 个配置键常量；类型 `MatchRequest{Version,ClientID,Platform,Channel,Region}` / `MatchResult{Matched,Reason,Rate,Bucket}`；`internal/handler/client.go` `ClientVersion` 升级：请求 DTO 扩展 `Region`/`Channel`/`HWID`/`DeviceID`，查询改为 `Find` 按 id DESC 遍历所有 active 版本，ClientID 解析优先级 `hwid > device_id > client_ip`，调用 `grayscale.Match` 首个命中即返回，响应扩展 `release_strategy`/`grayscale_hit`/`grayscale_bucket`/`grayscale_rate`（仅 grayscale/canary 命中时），全部未命中返回 5011；`internal/handler/tenant_business.go` `TenantCreateVersion` 升级 DTO（BackupURL/ReleaseStrategy/GrayscaleRate/GrayscalePlatforms/GrayscaleRegions/GrayscaleChannels）+ grayscale/canary + rate=0 自动取 `DefaultRate`；新增 `TenantUpdateVersion` PUT `/tenant/versions/:id`（归属校验 + 指针字段可选更新 + 切换灰度策略 + rate=0 取 `DefaultRate`）；`internal/handler/admin_business.go` 新增 `AdminListVersions` GET `/admin/versions`（JOIN sys_tenant + app，支持 tenant_id/app_id/channel/release_strategy 筛选）+ `AdminGetVersion` GET `/admin/versions/:id`；`internal/router/router.go` 注册 3 条新路由（adminAuth 2 + tenantAuth 1）；`internal/grayscale/grayscale_test.go` 33 个测试全 PASS（Match full 3 + 全局开关 1 + 平台过滤 4 + 渠道过滤 3 + 地区过滤 3 + 比例 4 + HashBucket 4 + ParseList 6 + DefaultRate/IsEnabled 4 + 边界 4）；12 个测试包全绿无回归，`go vet ./...` + `go build ./...` 通过；详见 SPEC 7.4.2 灰度发布体系规范）。

> v0.4.0 已落地：在线更新体系完成（`migrations/011_v0.4.0_online_update.up.sql` 新建 `system_update_log` 表 14 字段 + 3 索引 + 8 项 sys_config；`internal/config/cache.go` 新增 `RedisClient()` 方法暴露底层 Redis 客户端供 update 包实现分布式锁；新建 `internal/update` 包：`VerifyWebhookSignature` HMAC-SHA256 校验（`hmac.Equal` 防时序攻击 + 空 secret 跳过仅本地开发）+ `ParsePushEvent` push event 解析 + `BranchMatches` refs/heads/ 规范化 + `Manager.AcquireLock`/`ReleaseLock` 双重锁（进程内 mutex TryLock + Redis SET NX EX）+ `Manager.HealthCheck` HTTP GET（2xx/3xx 视为成功 + CheckRedirect 禁用跟随捕获原始 3xx + 超时控制）+ `Manager.ExecuteUpdate` 6 步流程（加锁→pending 审计日志→git fetch+reset --hard→bash 部署脚本→健康检查→失败 maybeRollback 自动回滚）+ `Manager.Rollback` 回滚到 commit_before + 状态查询 3 函数；常量 8 个配置键 + 3 个 TriggerSource + 5 个 Status + 3 个 StepStatus + 4 个 StepName；类型 `PushEvent`/`StepResult`/`UpdateOptions`/`UpdateResult`；新建 `internal/handler/update.go` 6 个 handler：`GitHubWebhook` POST /public/update/webhook（无鉴权+HMAC 签名）+ `AdminUpdateStatus` + `AdminTriggerUpdate` + `AdminListUpdateHistory` + `AdminRollbackUpdate` + `AdminGetUpdateLog`；router 注册 6 条新路由（publicGroup 1 + adminAuth 5）；新建 `scripts/deploy_update.sh` 默认部署脚本（go mod download + go build + DEPLOY_MODE 环境变量自适应 systemd/docker/pm2/none + 严格 set -euo pipefail）；`internal/update/update_test.go` 37 个测试全 PASS（VerifyWebhookSignature 7 + ParsePushEvent 4 + BranchMatches 5 + AcquireLock/ReleaseLock 5 + HealthCheck 6 + 状态机常量 4 + IsAutoUpdateEnabled/IsLocked 4 + 边界 6 + 并发压力 1）；测试修复了一个真实 bug（Go HTTP client 默认跟随重定向导致 3xx 测试失败 → CheckRedirect 返回 http.ErrUseLastResponse 捕获原始状态码）；13 个测试包全绿无回归，`go vet ./...` + `go build ./...` 通过；详见 SPEC 7.4.3 在线更新体系规范）。

> v0.4.0 已落地：API 开放平台完成（`migrations/016_v0.4.0_openapi_platform.up.sql` 新建 `developer_api_token` + `webhook_endpoint` + `webhook_delivery` 三张表 + 11 索引 + 8 项 openapi.*/webhook.* sys_config（`openapi.token_ttl_days`=365 / `openapi.token_max_per_tenant`=20 / `openapi.token_name_max_length`=64 / `openapi.request_timeout_seconds`=30 / `webhook.retry.max_attempts`=5 / `webhook.retry.backoff_seconds`=120 / `webhook.endpoint.disable_threshold`=10 / `webhook.delivery.retention_days`=30）；`internal/model/model.go` 新增 3 个 struct（`DeveloperAPIToken`/`WebhookEndpoint`/`WebhookDelivery`）；新建 `internal/openapi` 包：`TokenManager` 4 方法 `GenerateToken`（`crypto/rand` 生成 32 字节随机 → base62 编码 → `prefix_xxxxxxxx` 形式 → SHA-512 哈希存储 token_hash，DB 不存明文 + 前 8 位明文 prefix 用于展示识别 + 同租户数量上限校验 + 可选 TTL）+ `ValidateToken`（SHA-512 哈希后 DB 查找 + status=active + revoked_at IS NULL + expires_at 校验 + 异步更新 last_used_at/last_used_ip）+ `RevokeToken`（标记 status=revoked + revoked_at）+ `ListTokens`/`GetToken`；`WebhookManager` 6 方法 `CreateEndpoint`（URL https 强制 + events 非空 + AES-256-GCM 加密 secret 入库 secret_enc，DB 不存明文）+ `UpdateEndpoint`/`DeleteEndpoint`/`GetEndpoint`/`ListEndpoints` + `DispatchEvent`（按 event_type 查询订阅的 active endpoints → 异步分发到每个 endpoint：写 pending delivery → HMAC-SHA256(secret, event_id|timestamp|payload) 签名 + `hmac.Equal` 常量时间比较防时序攻击 + HTTP POST + 2xx 成功标记 delivered + 非 2xx 触发退避重试 next_retry_at = now + attempt * backoff_seconds + 连续失败 failure_count++，达 disable_threshold 自动 status=disabled）+ `RetryDelivery`（重新分发指定 delivery）+ `ProcessPendingRetries`（定时扫描 next_retry_at <= now 的 pending delivery 重试，达 max_attempts 标记 failed）；8 个预定义 Scope 常量（`ScopeCardRead`/`ScopeCardWrite`/`ScopeOrderRead`/`ScopeOrderWrite`/`ScopeAgentRead`/`ScopeAgentWrite`/`ScopeWebhookRead`/`ScopeWebhookWrite`）+ `HasScope` 工具 + `ParseScopes`/`FormatScopes` 转换 + 5 个事件类型常量（`EventOrderPaid`/`EventCardGenerated`/`EventAgentRegistered`/`EventAgentRechargeApproved`/`EventAgentWithdrawPaid`）；类型 `DeveloperAPIToken`/`WebhookEndpoint`/`WebhookDelivery`/`TokenCreateResult`/`DeliveryResult`；新建 `internal/middleware/auth.go` 新增 `APITokenAuth(mgr)` 中间件：从 Authorization: Bearer 头解析 token + 调用 `TokenManager.ValidateToken` + 按 error 类型映射错误码（`ErrTokenRevoked` 已撤销 / `ErrTokenExpired` 已过期 / `ErrTokenNotFound` 无效）+ 注入 `api_token_id`/`api_tenant_id`/`api_scopes`/`api_token_name` 到 gin.Context + `RequireScope(scopes...)` 中间件：基于 `HasScope` 校验任一 scope 命中即放行（与 JWTAuth 内部三角色鉴权 + H5EndUserAuth 终端用户鉴权 + SignatureAuth 客户端验证鉴权分离，构成四套独立鉴权体系）；新建 `internal/handler/openapi.go`：15 个端点（1 admin：`AdminOpenAPIStatus` GET /admin/openapi/status 平台总览统计（按 tenant 聚合 token 数 + endpoint 数 + delivery 数 + 24h 成功/失败统计）+ 13 tenant：Token CRUD 4（`TenantListAPITokens`/`TenantCreateAPIToken` 创建后返回明文仅一次 + `TenantGetAPIToken`/`TenantRevokeAPIToken`）+ Webhook endpoint CRUD 5（`TenantListWebhookEndpoints`/`TenantCreateWebhookEndpoint`/`TenantGetWebhookEndpoint`/`TenantUpdateWebhookEndpoint`/`TenantDeleteWebhookEndpoint`）+ Delivery 列表/详情/重试 3（`TenantListWebhookDeliveries`/`TenantGetWebhookDelivery`/`TenantRetryWebhookDelivery`）+ Meta 元信息 1（`TenantOpenAPIMeta` 返回可用 scope 列表 + 事件类型列表 + 配置项）+ 1 openapi：`OpenAPIWhoami` GET /api/v1/openapi/whoami 第三方调用方自校验返回 token_name + tenant_id + scopes）+ `DispatchWebhookEvent(deps, tenantID, eventType, payload)` 异步分发辅助函数（goroutine + panic recover + context.Background + best-effort，供 5 个业务点接入）；5 个业务点接入 Webhook 事件分发：`internal/handler/card.go` `TenantGenerateCards` 成功后 card.generated（仅批次级元信息，不含卡密明文，防泄漏）+ `internal/handler/pay.go` `processPaidOrder` 成功后 order.paid + `internal/handler/pay.go` `processAgentRegisterPaid` 成功后 agent.registered（事务外捕获 agentID 防止 tx 内 ID 失真）+ `internal/handler/tenant_finance.go` `TenantApproveRecharge` 成功后 agent.recharge.approved + `internal/handler/tenant_finance.go` `TenantPayWithdraw` 成功后 agent.withdraw.paid；router 注册 15 条新路由（adminAuth 1 + tenantAuth 13 + 新增 `openapiAuth := v1.Group("/openapi")` 路由组挂载 `middleware.APITokenAuth(tokenMgr)` 1）；`internal/openapi/openapi_test.go` 61 个测试全 PASS（TokenManager GenerateToken 6：正常/无 TTL/数量超限/名称超长/空名称/rand 失败 mock；ValidateToken 8：有效/已撤销/已过期/不存在/last_used 异步更新/空 token/IP 记录/边界；RevokeToken 3：成功/不存在/已撤销；ListTokens/GetToken 4：列表/分页/详情/越权；WebhookManager CreateEndpoint 5：正常/URL 非 https 拒绝/events 空拒绝/AES 加密验证/重复 URL；UpdateEndpoint 3：成功/不存在/越权；DeleteEndpoint 2：成功/不存在；ListEndpoints 2：列表/筛选；DispatchEvent 8：无订阅/单 endpoint/多 endpoint/endpoint disabled 跳过/HTTP 失败退避/连续失败阈值 disable/HMAC 签名验证/异步分发不阻塞；RetryDelivery 3：成功/达上限/不存在；ProcessPendingRetries 3：扫描重试/达 max_attempts 标记 failed/无 pending；Scope 工具 4：HasScope 命中/未命中/空 scopes/ParseScopes 边界；常量 4：8 个 scope 互不冲突/5 个事件类型互不冲突/配置键前缀；HMAC-SHA256 签名验证 2：签名生成正确/篡改 payload 拒绝；AES-256-GCM 加解密 2：secret 加密 round-trip/不同 tenant 隔离；hex 解码 2：SHA-512 hash 长度一致/不同输入 hash 不同）；测试修复了 2 个编译/逻辑错误：① `openapi_test.go` 缺少 `crypto/sha512` 导入导致 `sha512.Sum512` undefined ② `ParseScopes(",,,")` 返回 nil 与测试期望 `[]string{}` 不一致，改 `result := []string{}` 初始化非 nil；16 个测试包全绿无回归，`go vet ./...` + `go build ./...` 通过；详见 SPEC 7.4 API 开放平台规范）。
